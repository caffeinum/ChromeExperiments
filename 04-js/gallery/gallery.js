const initGallery = function () {
  
}

const selectPhoto = function (index) {
  // assign css
}

window.onload = function () {
  // init gallery:
  // - load all pictures to DOM
  // - set current picture

  // assign click handlers
  // re-draw gallery state
}
